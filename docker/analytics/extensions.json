{
  "extensions": [
    {
      "name": "pg_duckdb",
      "package": "postgresql-17-pg-duckdb",
      "description": "DuckDB-powered Postgres for high performance analytics",
      "preload_required": true,
      "test_enabled": true,
      "test_commands": [
        "CREATE EXTENSION IF NOT EXISTS \"pg_duckdb\";",
        "CREATE TEMP TABLE test_duckdb (id INT, name TEXT) USING duckdb;",
        "INSERT INTO test_duckdb VALUES (1, 'test');",
        "SELECT * FROM test_duckdb;"
      ]
    }
  ],
  "postgres_config": []
}